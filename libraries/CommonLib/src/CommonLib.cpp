#include "CommonLib.h"

namespace lib {

	/*
bool operator< (const DeviceSlot& lhs, const DeviceSlot& rhs) const
{
	return lhs.FlatId() < rhs.FlatId();
}

bool operator== (const DeviceSlot& lhs, const DeviceSlot& rhs) const
{
	return lhs.address == rhs.address && lhs.slot == rhs.slot;
}*/

} // namespace lib